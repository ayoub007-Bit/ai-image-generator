import React from 'react';
import { useNavigate } from 'react-router-dom'; // permet de naviguer par programmation vers une autre page sans utiliser un link (redirige vers ...)

import { preview } from '../assets';
import { getRandomPrompt } from '../utils';
import { FormField, Loader } from '../components';

const CreatePost = () => {
  return (
    <div>CreatePost</div>
  )
}

export default CreatePost